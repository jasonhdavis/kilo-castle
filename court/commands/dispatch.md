---
description: Dispatch a Serf session into a Chartered (PLANNED) Quest worktree
agent: steward
---
Quest: $ARGUMENTS

Follow `.kilo/prompts/steward.md` §"6. Dispatch" to dispatch a Serf into the target Quest worktree using `.court/templates/serf_dispatch_prompt.md`. The Quest must already be `PLANNED` (Chartered via `/charter` or `/plot`'s Council) with Goal & Scope and Expected Tribute filled in — if not, redirect to `/quest`/`/charter` first.
