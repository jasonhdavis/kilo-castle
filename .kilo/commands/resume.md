---
description: Resume a HELD Quest back into WORKING
agent: steward
---
Quest: $ARGUMENTS

Advance the Quest from `HELD` back to `WORKING` using `court advance <id> WORKING --note "Resumed after Audience decision"`.
